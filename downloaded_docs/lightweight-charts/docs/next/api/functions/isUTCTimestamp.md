Version: Next

On this page

> **isUTCTimestamp**(`time`): `time is UTCTimestamp`

Check if a time value is a UTC timestamp number.

## Parameters[​](#parameters "Direct link to Parameters")

• **time**: [`Time`](/lightweight-charts/docs/next/api/type-aliases/Time)

The time to check.

## Returns[​](#returns "Direct link to Returns")

`time is UTCTimestamp`

`true` if `time` is a [UTCTimestamp](/lightweight-charts/docs/next/api/type-aliases/UTCTimestamp) number, false otherwise.