##### [buffers\_adaptor::max\_size](max_size.html "buffers_adaptor::max_size")

Return the maximum number of bytes, both readable and writable, that can
ever be held.

###### [Synopsis](max_size.html#beast.ref.boost__beast__buffers_adaptor.max_size.synopsis)

```programlisting
std::size_t
max_size() const;
```