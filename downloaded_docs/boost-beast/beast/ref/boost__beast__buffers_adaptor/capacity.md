##### [buffers\_adaptor::capacity](capacity.html "buffers_adaptor::capacity")

Return the maximum number of bytes, both readable and writable, that can
be held without requiring an allocation.

###### [Synopsis](capacity.html#beast.ref.boost__beast__buffers_adaptor.capacity.synopsis)

```programlisting
std::size_t
capacity() const;
```