##### [http::token\_list::cbegin](cbegin.html "http::token_list::cbegin")

Return a const iterator to the beginning of the list.

###### [Synopsis](cbegin.html#beast.ref.boost__beast__http__token_list.cbegin.synopsis)

```programlisting
const_iterator
cbegin() const;
```