#### [http::to\_string](boost__beast__http__to_string.html "http::to_string")

Returns the text representation of a request method verb.

```programlisting
string_view
to_string(
    verb v);
  » more...
```

Convert a field enum to a string.

```programlisting
string_view
to_string(
    field f);
  » more...
```