##### [http::parser::merge\_all\_trailers](merge_all_trailers.html "http::parser::merge_all_trailers")

Returns `true` if the parser
is allowed to merge all trailer fields.

```programlisting
bool
merge_all_trailers() const;
  » more...
```

Set whether the parser is allowed to merge all trailer fields.

```programlisting
void
merge_all_trailers(
    bool v);
  » more...
```