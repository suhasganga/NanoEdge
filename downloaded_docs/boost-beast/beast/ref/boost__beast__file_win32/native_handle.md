##### [file\_win32::native\_handle](native_handle.html "file_win32::native_handle")

Returns the native handle associated with the file.

```programlisting
native_handle_type
native_handle();
  » more...
```

Set the native handle associated with the file.

```programlisting
void
native_handle(
    native_handle_type h);
  » more...
```