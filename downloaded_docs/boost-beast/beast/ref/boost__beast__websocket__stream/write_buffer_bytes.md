##### [websocket::stream::write\_buffer\_bytes](write_buffer_bytes.html "websocket::stream::write_buffer_bytes")

Set the write buffer size option.

```programlisting
void
write_buffer_bytes(
    std::size_t amount);
  » more...
```

Returns the size of the write buffer.

```programlisting
std::size_t
write_buffer_bytes() const;
  » more...
```