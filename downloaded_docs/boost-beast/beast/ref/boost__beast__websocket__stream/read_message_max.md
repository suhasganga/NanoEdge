##### [websocket::stream::read\_message\_max](read_message_max.html "websocket::stream::read_message_max")

Set the maximum incoming message size option.

```programlisting
void
read_message_max(
    std::size_t amount);
  » more...
```

Returns the maximum incoming message size setting.

```programlisting
std::size_t
read_message_max() const;
  » more...
```