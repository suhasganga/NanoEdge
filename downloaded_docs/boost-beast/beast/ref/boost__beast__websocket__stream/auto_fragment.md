##### [websocket::stream::auto\_fragment](auto_fragment.html "websocket::stream::auto_fragment")

Set the automatic fragmentation option.

```programlisting
void
auto_fragment(
    bool value);
  » more...
```

Returns `true` if the automatic
fragmentation option is set.

```programlisting
bool
auto_fragment() const;
  » more...
```