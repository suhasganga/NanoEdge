##### [websocket::stream::binary](binary.html "websocket::stream::binary")

Set the binary message write option.

```programlisting
void
binary(
    bool value);
  » more...
```

Returns `true` if the binary
message write option is set.

```programlisting
bool
binary() const;
  » more...
```