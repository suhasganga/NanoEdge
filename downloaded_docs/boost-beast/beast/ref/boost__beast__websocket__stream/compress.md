##### [websocket::stream::compress](compress.html "websocket::stream::compress")

Set the compress message write option.

```programlisting
void
compress(
    bool value);
  » more...
```

Returns `true` if the compress
message write option is set.

```programlisting
bool
compress() const;
  » more...
```