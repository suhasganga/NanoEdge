##### [websocket::stream::pong](pong.html "websocket::stream::pong")

Send a websocket pong control frame.

```programlisting
void
pong(
    ping_data const& payload);
  » more...

void
pong(
    ping_data const& payload,
    error_code& ec);
  » more...
```