##### [websocket::stream::text](text.html "websocket::stream::text")

Set the text message write option.

```programlisting
void
text(
    bool value);
  » more...
```

Returns `true` if the text message
write option is set.

```programlisting
bool
text() const;
  » more...
```