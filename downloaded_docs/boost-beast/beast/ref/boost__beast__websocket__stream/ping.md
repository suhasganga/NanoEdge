##### [websocket::stream::ping](ping.html "websocket::stream::ping")

Send a websocket ping control frame.

```programlisting
void
ping(
    ping_data const& payload);
  » more...

void
ping(
    ping_data const& payload,
    error_code& ec);
  » more...
```