##### [http::message::chunked](chunked.html "http::message::chunked")

Returns `true` if the chunked
Transfer-Encoding is specified.

```programlisting
bool
chunked() const;
  » more...
```

Set or clear the chunked Transfer-Encoding.

```programlisting
void
chunked(
    bool value);
  » more...
```