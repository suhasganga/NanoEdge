##### [http::message::contains](contains.html "http::message::contains")

Returns `true` if there is a
field with the specified name.

```programlisting
bool
contains(
    field name) const;
  » more...

bool
contains(
    string_view name) const;
  » more...
```