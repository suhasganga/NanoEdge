##### [http::message::keep\_alive](keep_alive.html "http::message::keep_alive")

Returns `true` if the message
semantics indicate keep-alive.

```programlisting
bool
keep_alive() const;
  » more...
```

Set the keep-alive message semantic option.

```programlisting
void
keep_alive(
    bool value);
  » more...
```