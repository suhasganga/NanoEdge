##### [buffer\_ref::capacity](capacity.html "buffer_ref::capacity")

Return the maximum number of bytes, both readable and writable, that can
be held without requiring an allocation.

###### [Synopsis](capacity.html#beast.ref.boost__beast__buffer_ref.capacity.synopsis)

```programlisting
std::size_t
capacity() const;
```