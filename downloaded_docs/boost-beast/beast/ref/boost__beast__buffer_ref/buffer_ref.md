##### [buffer\_ref::buffer\_ref](buffer_ref.html "buffer_ref::buffer_ref")

Create a buffer reference around `buffer`.

```programlisting
buffer_ref(
    Buffer& buffer);
  » more...
```

Copy the reference.

```programlisting
buffer_ref(
    const buffer_ref& buffer);
  » more...
```