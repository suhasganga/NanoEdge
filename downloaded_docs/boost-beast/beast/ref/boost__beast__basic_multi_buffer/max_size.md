##### [basic\_multi\_buffer::max\_size](max_size.html "basic_multi_buffer::max_size")

Set the maximum allowed capacity.

```programlisting
void
max_size(
    std::size_t n);
  » more...
```

Return the maximum number of bytes, both readable and writable, that can
ever be held.

```programlisting
size_type
max_size() const;
  » more...
```