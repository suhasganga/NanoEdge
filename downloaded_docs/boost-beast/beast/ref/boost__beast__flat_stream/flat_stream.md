##### [flat\_stream::flat\_stream](flat_stream.html "flat_stream::flat_stream")

```programlisting
flat_stream(
    flat_stream&&);
  » more...

flat_stream(
    flat_stream const&);
  » more...
```

Constructor.

```programlisting
template<
    class... Args>
explicit
flat_stream(
    Args&&... args);
  » more...
```