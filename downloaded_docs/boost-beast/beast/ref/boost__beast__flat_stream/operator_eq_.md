##### [flat\_stream::operator=](operator_eq_.html "flat_stream::operator=")

```programlisting
flat_stream&
operator=(
    flat_stream&&);
  » more...

flat_stream&
operator=(
    flat_stream const&);
  » more...
```