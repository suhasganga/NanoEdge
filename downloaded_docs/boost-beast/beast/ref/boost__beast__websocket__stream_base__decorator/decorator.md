##### [websocket::stream\_base::decorator::decorator](decorator.html "websocket::stream_base::decorator::decorator")

```programlisting
decorator(
    decorator&&);
  » more...
```

Construct a decorator option.

```programlisting
template<
    class Decorator>
explicit
decorator(
    Decorator&& f);
  » more...
```