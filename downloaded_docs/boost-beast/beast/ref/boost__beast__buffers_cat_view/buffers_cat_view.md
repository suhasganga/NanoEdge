##### [buffers\_cat\_view::buffers\_cat\_view](buffers_cat_view.html "buffers_cat_view::buffers_cat_view")

Copy Constructor.

```programlisting
buffers_cat_view(
    buffers_cat_view const&);
  » more...
```

Constructor.

```programlisting
explicit
buffers_cat_view(
    Buffers const&... buffers);
  » more...
```