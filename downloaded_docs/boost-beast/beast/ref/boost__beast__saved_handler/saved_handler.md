##### [saved\_handler::saved\_handler](saved_handler.html "saved_handler::saved_handler")

Default Constructor.

```programlisting
saved_handler();
  » more...
```

Copy Constructor (deleted)

```programlisting
saved_handler(
    saved_handler const&) = delete;
  » more...
```

Move Constructor.

```programlisting
saved_handler(
    saved_handler&& other);
  » more...
```