##### [test::fail\_count::fail\_count](fail_count.html "test::fail_count::fail_count")

```programlisting
fail_count(
    fail_count&&);
  » more...
```

Construct a counter.

```programlisting
explicit
fail_count(
    std::size_t n,
    error_code ev = error::test_failure);
  » more...
```