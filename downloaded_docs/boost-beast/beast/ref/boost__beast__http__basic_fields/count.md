##### [http::basic\_fields::count](count.html "http::basic_fields::count")

Return the number of fields with the specified name.

```programlisting
std::size_t
count(
    field name) const;
  » more...

std::size_t
count(
    string_view name) const;
  » more...
```