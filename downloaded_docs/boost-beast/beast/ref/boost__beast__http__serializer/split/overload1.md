###### [http::serializer::split (1 of 2 overloads)](overload1.html "http::serializer::split (1 of 2 overloads)")

Returns `true` if we will
pause after writing the complete header.

###### [Synopsis](overload1.html#beast.ref.boost__beast__http__serializer.split.overload1.synopsis)

```programlisting
bool
split();
```