##### [http::serializer::split](split.html "http::serializer::split")

Returns `true` if we will pause
after writing the complete header.

```programlisting
bool
split();
  » more...
```

Set whether the header and body are written separately.

```programlisting
void
split(
    bool v);
  » more...
```