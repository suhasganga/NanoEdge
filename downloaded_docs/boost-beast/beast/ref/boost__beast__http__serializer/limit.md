##### [http::serializer::limit](limit.html "http::serializer::limit")

Returns the serialized buffer size limit.

```programlisting
std::size_t
limit();
  » more...
```

Set the serialized buffer size limit.

```programlisting
void
limit(
    std::size_t limit);
  » more...
```