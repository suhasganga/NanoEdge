##### [static\_buffer\_base::capacity](capacity.html "static_buffer_base::capacity")

Return the maximum number of bytes, both readable and writable, that can
be held without requiring an allocation.

###### [Synopsis](capacity.html#beast.ref.boost__beast__static_buffer_base.capacity.synopsis)

```programlisting
std::size_t
capacity() const;
```