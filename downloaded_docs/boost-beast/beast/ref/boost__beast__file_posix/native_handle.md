##### [file\_posix::native\_handle](native_handle.html "file_posix::native_handle")

Returns the native handle associated with the file.

```programlisting
native_handle_type
native_handle() const;
  » more...
```

Set the native handle associated with the file.

```programlisting
void
native_handle(
    native_handle_type fd);
  » more...
```