##### [http::header::reason](reason.html "http::header::reason")

Return the response reason-phrase.

```programlisting
string_view
reason() const;
  » more...
```

Set the response reason-phrase (deprecated)

```programlisting
void
reason(
    string_view s);
  » more...
```