##### [http::header::header](header.html "http::header::header")

Constructor.

```programlisting
header();
  » more...

header(
    header&&);
  » more...

header(
    header const&);
  » more...

template<
    class... Args>
explicit
header(
    Args&&... args);
  » more...
```