##### [file\_stdio::native\_handle](native_handle.html "file_stdio::native_handle")

Returns the native handle associated with the file.

```programlisting
std::FILE*
native_handle() const;
  » more...
```

Set the native handle associated with the file.

```programlisting
void
native_handle(
    std::FILE* f);
  » more...
```