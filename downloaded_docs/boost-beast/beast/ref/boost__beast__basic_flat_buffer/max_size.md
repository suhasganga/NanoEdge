##### [basic\_flat\_buffer::max\_size](max_size.html "basic_flat_buffer::max_size")

Set the maximum allowed capacity.

```programlisting
void
max_size(
    std::size_t n);
  » more...
```

Return the maximum number of bytes, both readable and writable, that can
ever be held.

```programlisting
std::size_t
max_size() const;
  » more...
```