##### [buffered\_read\_stream::buffered\_read\_stream](buffered_read_stream.html "buffered_read_stream::buffered_read_stream")

Move constructor.

```programlisting
buffered_read_stream(
    buffered_read_stream&&);
  » more...
```

Construct the wrapping stream.

```programlisting
template<
    class... Args>
explicit
buffered_read_stream(
    Args&&... args);
  » more...
```