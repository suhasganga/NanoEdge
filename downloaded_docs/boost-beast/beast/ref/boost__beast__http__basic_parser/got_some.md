##### [http::basic\_parser::got\_some](got_some.html "http::basic_parser::got_some")

Returns `true` if the parser
has received at least one byte of input.

###### [Synopsis](got_some.html#beast.ref.boost__beast__http__basic_parser.got_some.synopsis)

```programlisting
bool
got_some() const;
```