##### [http::basic\_parser::eager](eager.html "http::basic_parser::eager")

Returns `true` if the eager
parse option is set.

```programlisting
bool
eager() const;
  » more...
```

Set the eager parse option.

```programlisting
void
eager(
    bool v);
  » more...
```