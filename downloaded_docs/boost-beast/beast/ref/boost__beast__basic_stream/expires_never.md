##### [basic\_stream::expires\_never](expires_never.html "basic_stream::expires_never")

Disable the timeout for subsequent logical operations.

###### [Synopsis](expires_never.html#beast.ref.boost__beast__basic_stream.expires_never.synopsis)

```programlisting
void
expires_never();
```